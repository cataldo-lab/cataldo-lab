```bash
# Conectar a MySQL como usuario root
mysql -u root -p

# Conectar a MySQL especificando el host y puerto
mysql -h localhost -P 3306 -u root -p

# Conectar a una base de datos específica
mysql -u root -p nombre_base_datos
